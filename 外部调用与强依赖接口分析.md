# 外部调用与强依赖接口分析（分析器说明）

你要求的判定方式是：

> **先找到外部依赖 Bean，然后从调用这些 Bean 的方法开始，层层递归往上分析，最终判断对外接口是否强依赖。**

当前脚本已按这个方向实现。

## 目标

- 自动扫描 SpringBoot 源码中的外部依赖与外部调用。
- 以“外部依赖 Bean/外部调用方法”为起点做**反向递归传播**。
- 输出哪些 Controller 接口对外部依赖构成强依赖，并给出原因。

## 运行

```bash
python3 spring_external_dependency_analyzer.py <project_root> -o analysis-report.json
```

## 判定规则（反向）

1. 识别外部依赖 Bean 类型（如 `RestTemplate`、`WebClient`、`@FeignClient`、MQ/Jdbc 等）。
2. 找到种子方法：
   - 直接包含外部调用语句；
   - 或调用了外部依赖 Bean 的方法。
3. 构建方法调用图后，沿反向边把“strong”标记递归传播到所有上层调用者。
4. 若接口入口方法属于 strong 集合，则该接口 `strong_dependency=true`。

## 输出要点

- `strong_methods`：所有强依赖方法（含 reasons）。
- `endpoints[].strong_dependency_reason`：接口命中的强依赖依据。
- `endpoints[].external_dependencies`：接口正向可达的直接外部调用点（辅助理解链路）。

## 说明

这仍是静态近似分析，不执行真实运行时容器行为；对于反射/AOP/动态代理等机制可能存在漏报或误报。
